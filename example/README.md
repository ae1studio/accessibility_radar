# accessibility_radar_example

Demo Flutter app for exercising the **Accessibility Radar** DevTools extension shipped from the parent package.

See the [repository README](../README.md) for `build_and_copy` and DevTools steps.

```bash
# from repo root
dart run devtools_extensions build_and_copy --source=. --dest=./extension/devtools

cd example && flutter run
```
